{
  "rules": {
    ".read": true,
    "users": {
      "$user_id": {
        ".write": "$user_id === auth.uid"
      }
    },
    "peers": {
      "$user_id": {
        ".write": "$user_id === auth.uid"
      }
    }
  }
}
